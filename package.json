{
  "name": "ant-supply-api",
  "version": "1.0.0",
  "description": "ANT Token Supply API for CoinMarketCap and CoinGecko",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "test": "node --test tests/pricing.test.mjs tests/supply.test.mjs tests/discovery.test.mjs",
    "bundle:check": "WRANGLER_SEND_METRICS=false npx --yes wrangler@4.127.0 versions upload --dry-run --outdir .wrangler/bundle"
  }
}
